import torch
import time

print("🔥 กำลังเตรียมทดสอบ GPU Stress Test...")

if torch.cuda.is_available():
    device = torch.device("cuda")
    print(f"✅ พบการ์ดจอ: {torch.cuda.get_device_name(0)}")
    
    # สร้างข้อมูลขนาดยักษ์โยนใส่การ์ดจอ
    print("🚀 กำลังโยนข้อมูลใส่ VRAM (ดู Task Manager ตอนนี้เลย!)...")
    
    # สร้าง Matrix ขนาดใหญ่ 2 อัน
    x = torch.randn(10000, 10000, device=device)
    y = torch.randn(10000, 10000, device=device)
    
    print("⚡ กำลังคำนวณคูณเลขมหาศาล (GPU ควรจะพุ่ง 100%)...")
    start = time.time()
    
    # สั่งให้คูณกัน 100 รอบ
    for i in range(100):
        z = torch.matmul(x, y)
    
    end = time.time()
    print(f"✅ เสร็จสิ้น! ใช้เวลา: {end - start:.2f} วินาที")
    print("ถ้าเวลาเร็วกว่า 10 วิ แปลว่า GPU ทำงานจริง!")
    
else:
    print("❌ ไม่พบ GPU! กรุณาลง PyTorch ใหม่")